#!/bin/bash
clientId="667b1df1bc944463953444aeb367da7d"
clientSecret="82870059-34b8-4d0f-a99a-bfe6e3d1a27e"
scope="em.mos.ext.services.idcs"
grantType="client_credentials"
oauthService="oauth-estage.oracle.com"
#combine the clientId and client Secret and encode them in base64 formatting
identity=`echo -n "${clientId}:${clientSecret}"|base64 -w0`
curl --noproxy ${oauthService} -H "Authorization: Basic ${identity}" -H "Content-Type: application/x-www-form-urlencoded;charset=UTF-8" --request POST https://${oauthService}/ms_oauth/oauth2/endpoints/oauthservice/tokens -d "grant_type=${grantType}&scope=${scope}"
echo


analytics MART requests has been Marcelo Potiens marcelo.potiens@oracle.com 